{
  "name": "nethermindassignment",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint",
    "dev": "npm run prepare && node dist/fetcher.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@jest/types": "^28.1.3",
    "queue": "^6.0.2"
  },
  "devDependencies": {
    "@types/jest": "^28.1.6",
    "@types/node": "^14.11.2",
    "gts": "^3.1.0",
    "jest": "^28.1.3",
    "ts-jest": "^28.0.7",
    "typescript": "^4.0.3"
  }
}
